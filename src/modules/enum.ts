export enum EGender {
	Female = 0,
	Male = 1,
}

export enum ERole {
	Root = 0,
	Admin = 1,
	User = 2
}

export enum DiscountType {
	Percent = '%',
	VND = 'VNĐ',
}

export enum DebtType {
	Borrow = 1,
	PayUp = 2,
	Refund = 3,
}
